/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
    domains: ['cdn0.casamentos.com.br', 'i.pinimg.com', 'www.eutbem.com.br'], // Adicione o domínio permitido
  },
};

export default nextConfig;
